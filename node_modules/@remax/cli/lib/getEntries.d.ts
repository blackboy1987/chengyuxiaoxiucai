import { Options, EntryInfo, Entries } from '@remax/types';
import API from './API';
export declare function getPages(options: Options, api: API): EntryInfo[];
export default function getEntries(options: Options, api: API): Entries;
