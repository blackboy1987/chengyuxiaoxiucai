import { Options } from '@remax/types';
import * as webpack from 'webpack';
export declare function run(options: Options): webpack.Compiler;
export declare function build(argv: Pick<Options, 'target' | 'watch' | 'notify' | 'port' | 'analyze' | 'minimize'>): webpack.Compiler;
