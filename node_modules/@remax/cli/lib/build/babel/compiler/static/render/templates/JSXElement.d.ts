import * as t from '@babel/types';
import { NodePath } from '@babel/traverse';
import { RenderNode } from '../../types';
import API from '../../../../../../API';
/**
 *  创建 JSXElement 对应的模板
 *
 * @export
 * @param {RenderNode} node
 * @param {NodePath} path
 * @param {(Array<string | number>)} dataPath
 * @param {Function} createTemplate
 * @returns
 */
export default function JSXElement(api: API, node: RenderNode<t.JSXElement>, path: NodePath, dataPath: Array<string | number>, createTemplate: (api: API, child: any, path: NodePath, dataPath: Array<string | number>) => string): string;
