import * as t from '@babel/types';
import { NodePath } from '@babel/traverse';
/**
 * 处理 JSX 中的表达式，使其可数
 *
 * @export
 * @param {NodePath} path
 * @returns
 */
export default function (path: NodePath<t.JSXExpressionContainer>): void;
