import { NodePath } from '@babel/traverse';
import TemplateInfoMap from './TemplateInfoMap';
import { RenderNode } from '../../types';
import API from '../../../../../../API';
export declare const templateInfoMap: TemplateInfoMap;
/**
 * 创建单个原生模板
 *
 * @param element
 * @param {NodePath} path
 * @param {string} module
 * @param {(Array<string | number>)} dataPath
 * @returns {string}
 */
export declare function createTemplate(api: API, element: RenderNode, path: NodePath<any>, dataPath: Array<string | number>): string;
