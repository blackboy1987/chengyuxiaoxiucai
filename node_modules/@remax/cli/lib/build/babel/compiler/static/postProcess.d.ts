import * as t from '@babel/types';
import { NodePath } from '@babel/traverse';
/**
 * 在生成模板后，对 jsx 再处理
 * 用于帮助简化 React 生成 虚拟 dom
 *
 * @export
 * @returns
 */
export default function postProcess(): {
    visitor: {
        JSXElement: (path: NodePath<t.JSXElement>) => false | undefined;
    };
};
