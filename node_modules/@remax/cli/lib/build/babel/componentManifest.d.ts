import * as t from '@babel/types';
import { NodePath } from '@babel/traverse';
import API from '../../API';
import Config from 'webpack-chain';
export declare const SlotView: ComponentManifest;
export interface ComponentManifest {
    id: string;
    props: string[];
    additional?: boolean;
    type?: string;
}
export default function hostComponent(api: API, config: Config): {
    visitor: {
        JSXElement: (path: NodePath<t.JSXElement>, state: any) => void;
    };
};
export declare function values(api: API): ComponentManifest[];
