import { Options, EntryInfo } from '@remax/types';
export declare function validate(route: string, options: Options): boolean;
export declare function filter(pages: EntryInfo[], options: Options): EntryInfo[];
