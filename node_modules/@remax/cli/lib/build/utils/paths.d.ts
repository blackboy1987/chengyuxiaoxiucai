import { Options, Platform } from '@remax/types';
import API from '../../API';
export declare function searchJSFile(file: string, target: Platform): string;
export declare function appConfigFile(options: Options): string;
export declare function pageConfigFile(pageFile: string, options: Options): string;
export declare function pageConfigFiles(options: Options, api: API): string[];
