import { Options, EntryInfo } from '@remax/types';
import API from '../../API';
export declare function generatePageRoutesInfo(options: Options, pages: EntryInfo[], api: API): {
    originalPath: string;
    route: string;
    path: string;
    config: any;
}[];
export declare function entryName(options: Options): string;
