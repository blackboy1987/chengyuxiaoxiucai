import { Options } from '@remax/types';
import { Compiler } from 'webpack';
import API from '../API';
export default function watch(options: Options, api: API, compiler: Compiler, watcher: any, addEntry?: boolean): any;
