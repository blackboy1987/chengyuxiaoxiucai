"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.walk = void 0;
const fs_1 = require("fs");
const postcss_1 = __importDefault(require("postcss"));
const nativeComponent_1 = require("../../../utils/nativeComponent");
const output_1 = __importDefault(require("../../../utils/output"));
exports.walk = (filePath, cssPaths) => {
    if (!fs_1.existsSync(filePath)) {
        return;
    }
    cssPaths.add(filePath);
    const content = fs_1.readFileSync(filePath);
    const ast = postcss_1.default.parse(content);
    if (!ast.nodes) {
        return;
    }
    ast.nodes.forEach(node => {
        if (node.type === 'atrule' && node.name === 'import') {
            const file = nativeComponent_1.getPath(filePath, node.params.replace(/'|"/g, ''));
            if (!fs_1.existsSync(file)) {
                output_1.default.error(`文件 ${file} 不存在`);
                return;
            }
            exports.walk(file, cssPaths);
        }
    });
};
function style(api, id) {
    const cssPaths = new Set();
    const filePath = id.replace(/\.js$/, api.getMeta().style);
    exports.walk(filePath, cssPaths);
    return Array.from(cssPaths);
}
exports.default = style;
