import Config from 'webpack-chain';
import { Options } from '@remax/types';
export interface RuleConfig {
    name: string;
    test: RegExp;
    loader?: string;
    options?: any;
}
export declare function addCSSRule(webpackConfig: Config, options: Options, web: boolean, ruleConfig: RuleConfig): void;
export declare function cssConfig(webpackConfig: Config, options: Options, web: boolean): void;
