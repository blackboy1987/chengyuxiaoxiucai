import Config from 'webpack-chain';
import { Options } from '@remax/types';
import { Platform } from '@remax/types';
export default function baseConfig(config: Config, options: Options, target: Platform): Config;
