"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const path = __importStar(require("path"));
const ejs_1 = __importDefault(require("ejs"));
const macro_1 = require("@remax/macro");
const shared_1 = require("@remax/shared");
const constants_1 = require("../../../babel/compiler/static/constants");
const templates_1 = require("../../../babel/compiler/static/render/templates");
const createPageTemplate_1 = require("./createPageTemplate");
async function createTurboPageTemplate(api, options, pageFile, modules, meta, compilation) {
    const renderOptions = createPageTemplate_1.createRenderOptions(api, options, modules, compilation);
    const pagePath = shared_1.slash(pageFile).replace(shared_1.slash(path.join(options.cwd, options.rootDir)) + '/', '');
    const fileName = shared_1.slash(`${path.dirname(pagePath)}/${path.basename(pagePath, path.extname(pagePath))}${meta.template.extension}`);
    const templates = templates_1.templateInfoMap.values().filter(t => {
        return modules.includes(t.module);
    });
    const entries = templates_1.templateInfoMap
        .values()
        .filter((template) => template.isEntry && shared_1.slash(template.module) === pageFile);
    renderOptions.components = renderOptions.components.filter(c => {
        var _a;
        if (c.type !== 'native') {
            return true;
        }
        // native 组件要筛选属于当前 page 下的
        return !!((_a = macro_1.nativeComponents.get(c.id)) === null || _a === void 0 ? void 0 : _a.importers.find(importer => modules.includes(importer)));
    });
    renderOptions.templates = templates;
    renderOptions.entries = entries;
    renderOptions.TEMPLATE_ID = constants_1.TEMPLATE_ID;
    // 可以进入到 static compiler 中的都是配置好 staticEjs 的平台
    let source = await ejs_1.default.renderFile(meta.staticEjs.page, renderOptions, {
        rmWhitespace: options.compressTemplate,
    });
    // TODO 用 uglify 替代 compressTemplate
    /* istanbul ignore next */
    if (options.compressTemplate) {
        source = source.replace(/^\s*$(?:\r\n?|\n)/gm, '').replace(/\r\n|\n/g, ' ');
    }
    compilation.assets[fileName] = {
        source: () => source,
        size: () => Buffer.byteLength(source),
    };
}
exports.default = createTurboPageTemplate;
