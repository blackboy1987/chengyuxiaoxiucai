import { compilation } from 'webpack';
import { Options, Meta } from '@remax/types';
import API from '../../../../API';
export default function createTurboPageTemplate(api: API, options: Options, pageFile: string, modules: string[], meta: Meta, compilation: compilation.Compilation): Promise<void>;
