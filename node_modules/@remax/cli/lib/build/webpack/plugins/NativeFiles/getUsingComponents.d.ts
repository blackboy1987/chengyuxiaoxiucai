import { compilation } from 'webpack';
import { Options } from '@remax/types';
export default function getUsingComponents(modules: string[], options: Options, compilation: compilation.Compilation): any;
