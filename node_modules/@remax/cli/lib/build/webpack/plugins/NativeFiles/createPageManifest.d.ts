import { compilation } from 'webpack';
import { Options, EntryInfo } from '@remax/types';
import API from '../../../../API';
export default function createPageManifest(options: Options, page: EntryInfo, modules: string[], compilation: compilation.Compilation, api: API): void;
