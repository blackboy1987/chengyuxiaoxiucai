import { compilation } from 'webpack';
import { Options } from '@remax/types';
import API from '../../../../API';
export default function createAppManifest(options: Options, api: API, compilation: compilation.Compilation): void;
