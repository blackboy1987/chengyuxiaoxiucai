import { compilation } from 'webpack';
import { Options, Meta } from '@remax/types';
import * as componentManifest from '../../../../build/babel/componentManifest';
import API from '../../../../API';
export declare function createRenderOptions(api: API, options: Options, modules: string[], compilation: compilation.Compilation, filter?: boolean): {
    components: componentManifest.ComponentManifest[];
    slotView: {
        props: string[];
    };
};
export default function createPageTemplate(api: API, options: Options, modules: string[], pageFile: string, meta: Meta, compilation: compilation.Compilation): Promise<void>;
export declare function createBaseTemplate(api: API, options: Options, meta: Meta, compilation: compilation.Compilation): Promise<null | undefined>;
