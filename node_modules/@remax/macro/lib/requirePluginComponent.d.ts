import { NodePath } from '@babel/traverse';
import { NativeComponent } from '@remax/types';
export declare const nativeComponents: Map<string, NativeComponent>;
export declare function register(sourcePath: string, importer?: string, assets?: string[]): string;
export default function requirePluginComponent(path: NodePath, state: any): void;
