import { createHostComponent } from '@remax/shared';
export var Switch = createHostComponent('switch');
Switch.defaultProps = {
    checked: false,
    disabled: false,
    type: 'switch',
    color: '#F85959',
};
